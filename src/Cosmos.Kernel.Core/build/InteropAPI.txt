# There are some Interop.Sys methods that cannot be plugged directly or through their callers,
# so we add them here to enable static linking. 
# 
# Valid values are `<libname>` so all DllImports of lib are statically linked or `<libname>!<functionname>`
# for a specific function.
#
# For more information see: https://learn.microsoft.com/en-us/dotnet/core/deploying/native-aot/interop

libSystem.Native!SystemNative_SchedGetCpu
libSystem.Native!SystemNative_GetCpuUtilization